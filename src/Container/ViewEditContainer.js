/* eslint-disable prettier/prettier */
import { connect } from 'react-redux';
import ViewEdite from '../Screens/ScreenViewEdite';
const mapStateToProps = state => ({

})
const mapDispatchToProps = {
}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ViewEdite)